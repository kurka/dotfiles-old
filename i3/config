# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

set $OUTPUT_L VGA-1
set $OUTPUT_C DP-1
set $OUTPUT_R HDMI-2

# smart_gaps on
#gaps inner 10
# gaps outer 10
#smart_borders on


# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:System San Francisco Display 8
# font pango:Terminus 10px #monospace 8
#font pango:Roboto Mono 10px #monospace 8
#font pango:DejaVu Sans Mono, Terminus Semi-Condensed 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# http://i3wm.org/docs/userguide.html#_layout_mode_for_new_containers
#workspace_layout tabbed

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

#for_window [class="Guake"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="prefwindow"] floating enable
#for_window [class="^Skype$"] floating enable
for_window [class="notify"] floating enable border 1pixel
# for_window [class="update-manager"] floating enable border 1pixel
# for_window [class="gnome-calculator"] floating enable border 1pixel resize 640 480
#for_window [title="Kupfer"] floating enable border 1pixel
#for_window [class="terminal"] border 1pixel
#for_window [title="Pidgin"] move left; resize shrink right 31px or 31 ppt
#for_window [class="gnome-panel"] move to output $OUTPUT_E
#for_window [class="vlc"] floating enable
#for_window [class="^Roxterm$"] border 1pixel
new_window pixel 1

# Screen Shots
# bindsym --release $mod+x exec --no-startup-id import /tmp/latest-screenshot.png
bindsym Print exec scrot -e 'mv $f ~/Pictures/$f'
bindsym Mod1+Print exec scrot -u -e 'mv $f ~/Pictures/$f'
bindsym $mod+Print exec scrot




# start common programs
bindsym $mod+Return exec termite
bindsym $mod+o exec nautilus --no-desktop &
bindsym $mod+i exec termite -e vim

# kill focused window
bindsym $mod+Control+t kill
bindsym --release button2 kill

# start dmenu (a program launcher)
bindsym $mod+p exec rofi -show run -lines 3 -eh 2 -width 100 -padding 450 -opacity "85" -bw 0 -bc "$bg-color" -bg "$bg-color" -fg "$text-color" -hlbg "$bg-color" -hlfg "#9575cd" -font "System San Francisco Display 18"#"pango 18"
bindsym $mod+slash exec rofi -show window -lines 3 -eh 2 -width 100 -padding 450 -opacity "85" -bw 0 -bc "$bg-color" -bg "$bg-color" -fg "$text-color" -hlbg "$bg-color" -hlfg "#9575cd" -font "pango 18"
#bindsym $mod+Return exec rofi -show run
bindsym $mod+Control+Return exec dmenu_run
bindsym $mod+Shift+Return exec j4-dmenu-desktop
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
bindsym $mod+Mod1+Up exec "amixer -D pulse sset Master '5%+'"
bindsym $mod+Mod1+Down  exec "amixer -D pulse sset Master '5%-'"
bindsym $mod+Mod1+m exec "amixer -D pulse set Master toggle"



#bindsym $mod+Control+r        exec --no-startup-id i3menu root
#bindsym $mod+Shift+G  exec --no-startup-id i3menu goto_actions
# bindsym $mod+b        exec --no-startup-id i3menu bar_actions
#bindsym $mod+i        exec --no-startup-id i3menu global_actions
## bindsym $mod+s        exec --no-startup-id i3menu scratchpad_actions
## bindsym $mod+l        exec --no-startup-id i3menu layout
## bindsym $mod+w        exec --no-startup-id i3menu window_actions
#bindsym $mod+Shift+W  exec --no-startup-id i3menu target_window_actions
## bindsym $mod+k        exec --no-startup-id i3menu workspace_actions
#bindsym $mod+Shift+K  exec --no-startup-id i3menu target_workspace_actions


# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Resize Containers, Vim-style                            ($mod+Control+[hjkl])
# bindsym $mod+Control+Shift+j resize grow height 5 px or 5 ppt
# bindsym $mod+Control+Shift+k resize shrink height 5 px or 5 ppt
# bindsym $mod+Control+Shift+l resize grow width 5 px or 5 ppt
# bindsym $mod+Control+Shift+h resize shrink width 5 px or 5 ppt
bindsym $mod+Shift+plus resize grow width 5 px or 5 ppt
bindsym $mod+equal resize grow width 5 px or 5 ppt
bindsym $mod+Control+Shift+plus resize grow height 5 px or 5 ppt
bindsym $mod+Control+equal resize grow height 5 px or 5 ppt
bindsym $mod+minus resize shrink width 5 px or 5 ppt
bindsym $mod+Control+minus resize shrink height 5 px or 5 ppt





# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+backslash layout stacking
bindsym $mod+Control+backslash split h; layout stacking
bindsym $mod+z layout tabbed
bindsym $mod+Control+z split h; layout tabbed
bindsym $mod+x layout toggle split
bindsym $mod+b layout toggle all
# split in horizontal orientation
bindsym $mod+c split h
# split in vertical orientation
bindsym $mod+v split v

# toggle tiling / floating
bindsym $mod+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+A focus child

# switch to workspace
set $w1r 1:  webr
set $w2r 2:  devr
set $w3r 3:  readr
# set $w10 10: spotify
# set $w4r 4
# set $w5r 5
# set $w6r 6
# set $w7r 7
# set $w8r 8
# set $w9r 9
# set $w10r 10

set $w1c 1:  webc
set $w2c 2:  readc
set $w3c 3:  devc
# set $w4l 4
# set $w5l 5
# set $w6l 6
# set $w7l 7
# set $w8l 8
# set $w9l 9
# set $w10l 10

#exclam
#at
#numbersign
#dollar
#percent
#dead_circunflex
#ampersand
#asterisk
#parenleft
#parenright

bindsym $mod+n exec --no-startup-id ~/.config/i3/i3-new-workspace.sh

#right monitor workspaces:
bindsym $mod+1 workspace $w1c
bindsym $mod+2 workspace $w2c
bindsym $mod+3 workspace $w3c
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

#left monitor workspaces:
bindsym $mod+q workspace $w1r
bindsym $mod+w workspace $w2r
bindsym $mod+e workspace $w3r
# bindsym $mod+Shift+4 workspace $w4l
# bindsym $mod+Shift+5 workspace $w5l
# bindsym $mod+Shift+6 workspace $w6l
# bindsym $mod+Shift+7 workspace $w7l
# bindsym $mod+Shift+8 workspace $w8l
# bindsym $mod+Shift+9 workspace $w9l
# bindsym $mod+Shift+0 workspace $w10l


# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $w1c
bindsym $mod+Shift+2 move container to workspace $w2c
bindsym $mod+Shift+3 move container to workspace $w3c
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

bindsym $mod+Shift+q move container to workspace $w1r
bindsym $mod+Shift+w move container to workspace $w2r
bindsym $mod+Shift+e move container to workspace $w3r
# bindsym $mod+Shift+Control+4 move container to workspace $w4l
# bindsym $mod+Shift+Control+5 move container to workspace $w5l
# bindsym $mod+Shift+Control+6 move container to workspace $w6l
# bindsym $mod+Shift+Control+7 move container to workspace $w7l
# bindsym $mod+Shift+Control+8 move container to workspace $w8l
# bindsym $mod+Shift+Control+9 move container to workspace $w9l
# bindsym $mod+Shift+Control+0 move container to workspace $w10l

# Move and focus container
bindsym $mod+Control+1 move container to workspace $w1c; workspace $w1c
bindsym $mod+Control+2 move container to workspace $w2c; workspace $w2c
bindsym $mod+Control+3 move container to workspace $w3c; workspace $w3c
bindsym $mod+Control+4 move container to workspace 4; workspace 4
bindsym $mod+Control+5 move container to workspace 5; workspace 5
bindsym $mod+Control+6 move container to workspace 6; workspace 6
bindsym $mod+Control+7 move container to workspace 7; workspace 7
bindsym $mod+Control+8 move container to workspace 8; workspace 8
bindsym $mod+Control+9 move container to workspace 9; workspace 9
bindsym $mod+Control+0 move container to workspace 10; workspace 10

bindsym $mod+Control+q move container to workspace $w1r; workspace $w1r
bindsym $mod+Control+w move container to workspace $w2r; workspace $w2r
bindsym $mod+Control+e move container to workspace $w3r; workspace $w3r
# bindsym $mod+Control+4 move container to workspace $w4l
# bindsym $mod+Control+5 move container to workspace $w5l
# bindsym $mod+Control+6 move container to workspace $w6l
# bindsym $mod+Control+7 move container to workspace $w7l
# bindsym $mod+Control+8 move container to workspace $w8l
# bindsym $mod+Control+9 move container to workspace $w9l
# bindsym $mod+Control+0 move container to workspace $w10l


workspace "$w1r" output $OUTPUT_R
workspace "$w2r" output $OUTPUT_R
workspace "$w3r" output $OUTPUT_R

workspace "$w1c" output $OUTPUT_C
workspace "$w2c" output $OUTPUT_C
workspace "$w3c" output $OUTPUT_C

assign [class="Spotify"] → 10

# Rename workspace with i3-input using numbers and text
bindsym $mod+Control+n exec i3-input -F 'rename workspace to "%s"' -P 'New  name: '

# Switch to workspace with urgent window automatically
for_window [urgent=latest] focus

# Switching/moving workspaces
# workspace_auto_back_and_forth yes
bindsym Mod1+Tab workspace back_and_forth

bindsym $mod+Control+Left workspace prev_on_output
bindsym $mod+Control+h workspace prev_on_output

bindsym $mod+Control+Right workspace next_on_output
bindsym $mod+Control+l workspace next_on_output

bindsym $mod+Control+Shift+Left move workspace to output left
bindsym $mod+Control+Shift+Right move workspace to output right

# reload the configuration file
# bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Control+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 5 px or 5 ppt
        bindsym j resize grow height 5 px or 5 ppt
        bindsym k resize shrink height 5 px or 5 ppt
        bindsym l resize grow width 5 px or 5 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

bindsym $mod+F12 exec xscreensaver-command -lock

#Startup programs
exec --no-startup-id "xscreensaver -no-splash"
exec --no-startup-id "xrandr --output VGA-1 --mode 1280x1024 --pos 0x0 --rotate normal --output HDMI-2 --mode 1920x1080 --pos 3200x0 --rotate normal --output HDMI-1 --off --output DP-1 --mode 1920x1200 --pos 1280x0 --rotate normal --output DP-2 --off"
exec --no-startup-id dropbox
exec --no-startup-id rescuetime
exec --no-startup-id compton -b -f
exec --no-startup-id dunst &
exec --no-startup-id redshift &
exec --no-startup-id ~/gitwatch/giwatch.sh -s 14400 -r origin -m "%d" ~/vimwiki 
exec --no-startup-id setxkbmap -option caps:escape
# misc settings

# log out prompt
set $mode_system (l)ock, (e)logout, (r)eboot, Shift+(s)hutdown
mode "$mode_system" {
   bindsym l       exec $I3lock, mode "default"
   bindsym e       exec i3-msg exit
   bindsym r       exec notify-send "SYSTEM: Rebooting..." && systemctl reboot
   bindsym Shift+s exec notify-send "SYSTEM: Powering off..." && systemctl poweroff
   bindsym Return mode "default"
   bindsym Escape mode "default"
}
bindsym $mod+Control+y mode "$mode_system"

bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks
bindsym --release Num_Lock  exec pkill -SIGRTMIN+11 i3blocks


# scratchpad
bindsym $mod+Shift+dead_tilde move scratchpad
#bindsym $mod+Control+dead_tilde scratchpad show
bindsym $mod+dead_tilde scratchpad show
bindsym $mod+m [class="Spotify"] scratchpad show
bindsym $mod+comma [title=".*vimwiki.*"] scratchpad show
bindsym $mod+period [title=".*htop$"] scratchpad show


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
#    id bar-0
#    # tray_output primary
#    position top
#    # status_command ~/.config/i3/conky/conky-i3bar
#    status_command i3bar
#    colors {
#        background #24364F
#        statusline #CCCCCC
#        focused_workspace  #ffffff #285577
#        active_workspace   #cccccc #333333
#        inactive_workspace #999999 #222222
#        urgent_workspace   #ffffff #900000
#    }
# }

set $bg-color 	         #2f343f
set $inactive-bg-color   #2f343f
set $text-color          #f3f4f5
set $inactive-text-color #676E7D
set $urgent-bg-color     #E53935


bar {
  	status_command i3blocks -c ~/.config/i3/i3blocks/i3blocks.conf
    position top
	colors {
		background $bg-color
	    	separator #757575
		#                  border             background         text
		focused_workspace  $bg-color          $bg-color          $text-color
		inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
		urgent_workspace   $urgent-bg-color   $urgent-bg-color   $text-color
	}
}

#
#
# colors
# class                 border  backgr. text    indicator
# client.focused          #3498DB #208099 #ECF0F1 #2ECC71
# client.unfocused        #3498DB #102030 #ECF0F1 #2C3E50
# client.focused_inactive #3498DB #2C3E50 #ECF0F1 #2C3E50
# client.urgent           #3498DB #2C3E50 #ECF0F1 #2C3E50

client.focused          #849492 #849492 #ffffff #2e9ef4
client.focused_inactive #6e686f #6e686f #ffffff #484e50
client.unfocused        #6e686f #6e686f #888888 #292d2e
client.urgent           #e9e6e6 #BFB8B0 #ffffff #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000



set $base00 #101218
set $base01 #1f222d
set $base02 #252936
set $base03 #7780a1
set $base04 #C0C5CE
set $base05 #d1d4e0
set $base06 #C9CCDB
set $base07 #ffffff
set $base08 #ee829f
set $base09 #f99170
set $base0A #ffefcc
set $base0B #a5ffe1
set $base0C #97e0ff
set $base0D #97bbf7
set $base0E #c0b7f9
set $base0F #fcc09e


# Window Colours
#                         border  background text    indicator
# client.focused          $base01 $base01    $base07 $base0F
# client.focused_inactive $base02 $base02    $base03 $base0F
# client.unfocused        $base02 $base02    $base03 $base0F
# client.urgent           $base02 $base08    $base00 $base0F
